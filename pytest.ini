[pytest]
markers =
    unit: isolated unit tests of individual functions
    security: security-focused tests (auth, IDOR, rate limiting)
    integration: multi-endpoint / full user-journey tests
testpaths = tests
addopts = -ra --strict-markers
